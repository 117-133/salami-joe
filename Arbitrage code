#from sys import version

#import requests
# Import something which lets me know the current time
#def polymarketsstuff: #All of the functions for the polymarkets API
#r = requests.get("https://gamma-api.polymarket.com/events?closed=false") # Grab all polymarkets events
#response = r.json 
#def OddsAPIstuff: #All of the funcitons for OddsAPI
#c = 'currentdate'#get from time import


import keyboard
import time
seeed = float(input('How much do you have? '))
h = 2
while True: 
    

    odds1 = 100/float(input('First Value? '))
    odds2 = float(input('Second Value? '))
    x = 1/odds1 + 1/odds2
    if x < 1:
        print ('$$$$')
        comb = odds1+odds2 
        bet1 = seeed*odds2/comb
        bet2 = seeed*odds1/comb
        print (round(bet1, 2),round(bet2, 2))
        print (bet2/odds1)
    else:
        print('try again!')
    
    

       


    




#for event in response:
    #if date == c+1:
        #if 'sports' in 'eventname':
            #if 'soccer' or 'nfl' in 'eventname':
             #print('NO')
            #else:
                #home = 'hometeam'
                #away = 'awayteam'
                #p = 'oddshometeamwins'
                #m = 'oddsawayteamwins'
                #for eventodds in responseodds:
                    #if 'home' == homeodds, 'away'== 'awayodds', 'date' ==: 
                



# Filter events down to ones happening tmrw
# Filter events down to sports related ones(exc. sports that have draws(Soccer, NFL primarily))
# For each event, identify the home and away team, and pull out the current odds(Include the weird update thing)
# Pull out the odds for the same event from OddsAPI
# Run Arbitrage Calculations between odds, if successful then print current odds
# Maybe have a separate machine for fast-tracking updated odds
         


